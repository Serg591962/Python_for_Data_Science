Библиотека **matplotlib** - базовый инструмент для создания готового к публикации графика.
Рекомендованный интерфейс для **matplotlib** - **pyplot**, который по соглашению для удобства носит псевдоним plt:
**import matplotlib.pyplot as plt**
cd /d/wirt
mkdir /d/wirt/project/matplotlib_pyplot
"/d/Python/Python3_13_3/python.exe" -m venv /d/wirt/matplotlib_pyplot_env
source /d/wirt/matplotlib_pyplot_env/Scripts/activate
python -m pip install --upgrade pip setuptools wheel
pip install matplotlib
python -c "import matplotlib; print(matplotlib.__version__)" проверка версии matplotlib
deactivate
###### **Два основных понятия в matplotlib — это фигуры и оси:**
фигуры - это весь экран компьютера.
оси - часть экрана на которой строятся графики 
- FigureВсё полотно         (общее окно)	                                         fig, ax = plt.subplots()
- Axes	                                Область, где рисуется один график	ax.plot(...)
- Axis	                                Конкретная координатная ось (X или Y)
**Для создания фигур и осей** matplotlib предлагает два подхода: **явный и неявный.** 
**Неявный стиль** 
`plt.plot([1, 2, 3], [1, 4, 9])` - Построение первого графика: x-значения: [1, 2, 3] y-значения: [1, 4, 9]
matplotlib сам создаёт фигуру и ось,
`plt.plot([1, 2, 3], [9, 4, 1])` 
plt.title("Неявный") - Добавляет заголовок к текущему графику
plt.show() - Показывает окно с графиком
**Явный стиль**
fig, (ax1, ax2) = plt.subplots(1, 2)
- fig — это фигура (большой холст)  
- ax - это одна ось (область для рисования) (определяем что их будет две и их названия)
- plt - это сокращение для модуля `matplotlib.pyplot`.
- plt.subplots() — это функция, которая создаёт: fig, axes
- (1, 2) - получаем график в 1 строку и 2 столбца
`ax1.plot([1, 2, 3], [1, 4, 9])` - рисуем первый график **на первой оси (`ax1`)**
`ax2.plot([1, 2, 3], [9, 4, 1])`
ax1.set_title("первый") - это установка заголовка для конкретной оси (графика).
plt.show() - это команда вывести график на экран
###### 1. **Метод plt.plot - построение линейного графика**
import matplotlib.pyplot as plt
`X = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10]`
`Y = [20, 25, 35, 50, 10, 12, 20, 40, 70, 110]`
plt.plot(x, y)  # Построит график зависимости y от x
plt.title("Линейный график")
plt.xlabel("X") # Задаёт подпись для горизонтальной оси (X).
plt.ylabel("Y")
plt.show() # Команда вывести график на экран
###### 2. **Метод plt.hist - строит гистограмму, график распределения данных по интервалам (бинам)**
import matplotlib.pyplot as plt
X = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10]
Y = [20, 25, 35, 50, 10, 12, 20, 40, 70, 110]
plt.bar(X, Y, width=0.8, align='center')  # Построит столбчатую диаграмму
plt.title("Гистограмма с заданными значениями и частотами")
plt.xlabel("Значение")
plt.ylabel("Частота")
plt.show()
**Функция plt.bar()**
plt.bar() — это метод для построения столбчатых диаграмм (bar charts).
параметры имеют определённый порядок, то есть позиционные аргументы:plt.bar(x, height, ...)
X — координаты столбцов по горизонтальной оси (где рисовать столбцы).
Y — значения для параметра height. height — высоты столбцов (как высокие будут столбцы, обычно это частоты или значения).
width — ширина каждого столбца (по умолчанию равна 0.8). Если width=1, тогда каждый столбец занимает всю ширину между двумя целочисленными точками.
align — выравнивание столбцов относительно координат x.
##### **Оформление графиков**
##### **- Применение свойств класса matplotlib.Line2D. применяя как аргументы ключевого слова для plt.plot.**
###### **1. marker** - указывается с помощью ключевого слова marker (plt.plot(X, Y, marker='s'))
типы маркеров:
- . точка
- , пиксель 
- o круг
- v triangle_down 
- ^ triangle_up 
- < triangle_left 
- triangle_right 
- 1 tri_down
- 2 tri_up 
- 3 tri_left 
- 4 tri_right 
- s квадрат 
- p пентагон
* `* звезда` 
* h шестиугольник 1 
* H шестиугольник 2 
+ плюс 
+ x x 
+ D бриллиант 
+ d thin_diamond 
+ | vline _ hline
import matplotlib.pyplot as plt
`X = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10]`
`Y = [20, 25, 35, 50, 10, 12, 20, 40, 70, 110]`
plt.plot(X, Y, marker='s')  # Построит график зависимости y от x
plt.show() # Команда вывести график на экран
###### **2 стили линий:** используется ключевое слово linestyle
- `- сплошной стиль (solid)`
- -- штриховой стиль (dashed) 
- -. стиль штрих-пунктир (dash-dot)
- : пунктирный стиль (dotted)
###### **3. цвет:** используется ключевое слово color
- b синий 
- g зеленый 
- r красный 
- c голубой 
- m пурпурный 
- y желтый 
- k черный 
- w белый
##### **- Использование аргумента fmt.**  
Это позиционный параметр, который отображается справа от параметра Y. Он состоит из форматирующей строки, использующей условное обозначение для настроек маркера, стиля линии и цвета. 
X = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10] 
Y = [20, 25, 35, 50, 10, 12, 20, 40, 70, 110] 
fmt = 's-.r' (s квадрат, -. стиль штрих-пунктир (dash-dot), r красный  )
plt.plot(X, Y, fmt)
можно добавить linewidth=4.3 - ширина линии в графике, измеряется в пунктах (points), где 1.0 — это стандартная толщина.
fmt = 's-.r' 
plt.plot(X, Y, fmt, linewidth=4.3)
##### **Маркировка данных**
**Маркированные данные** — это данные, где у каждого значения есть **имя (метка)**, например:
- в **словаре**: ключи — это метки
- в **DataFrame**: названия столбцов — это метки
Доступ к данным осуществляется с помощью синтаксиса скобок. Вместо указания последовательности значений для x и y вы указываете соответствующие метки.
**Маркированный вариант -> plt.plot('Years', 'Women', data=heights_df):**
- ось X = значения из столбца `"Years"`
- ось Y = значения из столбца `"Women"`
- данные взять из таблицы `heights_df`
**Обычный вариант ->    x = heights_df["Years"]**
                    **y = heights_df["Women"]**
                     **plt.plot(x, y)**
Пример: 
import pandas as pd
import matplotlib.pyplot as plt
data = {"Years": ["2000", "2002", "2004", "2006", "2008", "2010", "2012", "2014", "2016"],
    "Men": [189.1, 191.8, 193.5, 196.0, 194.7, 196.3, 194.4, 197.0, 197.8],
    "Women": [175.7, 176.4, 176.5, 176.2, 175.9, 175.9, 175.7, 175.8, 175.3]}
heights_df = pd.DataFrame(data) - преобразует словарь data в таблицу (DataFrame)
plt.plot('Years', 'Women', data=heights_df) - строит график по названиям столбцов из таблицы
- **data=...**	Указывает, откуда брать эти столбцы (из DataFrame)
- **heights_df["Years"]**	Это обычный способ получения данных, но можно не писать вручную
plt.grid(True) - Добавляет сетку на график
plt.show() - Показывает график на экране (открывает окно)
##### **Построение графиков для множества наборов данных**
###### **Подход 1. Много вызовов plt.plot() по очереди**
X = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10]
Y = [20, 25, 35, 50, 10, 12, 20, 40, 70, 110]
fmt = 's-.r'  # квадратные маркеры, штрих-пунктирная линия, красный цвет

X1 = [0, 1, 2, 3, 4, 5, 7, 8, 9, 10]
Y2 = [90, 89, 87, 82, 72, 60, 45, 28, 10, 0]
fmt2 = '^k:'  # треугольники вверх, пунктирная линия, чёрный цвет

plt.plot(X, Y, fmt)
plt.plot(X1, Y2, fmt2)
plt.grid(True)
plt.show()
- `plt.plot()` вызывается дважды — два раза строится график.
- Оба графика отображаются **на одной и той же фигуре**, потому что `matplotlib` по умолчанию продолжает рисовать на текущем "поле".
###### **Подход 2. Один вызов plt.plot(...) с несколькими наборами данных**
plt.plot(X, Y, fmt, X1, Y2, fmt2)
- В plt.plot() ты передаёшь несколько X-Y-пар (и форматы для каждой).
- Это короче, чем писать plt.plot() дважды.
- Результат тот же — несколько линий на одном графике.
###### **Подход 3. Маркированные данные (через DataFrame)** - [[Labeled data via DataFrame]]
Ты не передаёшь X и Y как списки, а указываешь имена столбцов ("Years", "Women", "Men")
data=heights_df говорит matplotlib: ищи эти названия в этой таблице.
Рисуются две линии на одной фигуре:
- Одна по столбцу "Women"
- Другая по столбцу "Men"

